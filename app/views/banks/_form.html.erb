<%= form_with(model: bank) do |form| %>
  <% if bank.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bank.errors.count, "error") %> prohibited this bank from being saved:</h2>
      <ul>
        <% bank.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :bank_name, style: "display: block" %>
    <%= form.text_field :bank_name %>
  </div>
  <div>
    <%= form.label :bank_abbrv, style: "display: block" %>
    <%= form.text_field :bank_abbrv %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
